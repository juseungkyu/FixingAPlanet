export default class Draw {
    constructor() {
        this.init()
    }

    /**
     * @param {PointerEvent} event
     */
    onMouseDown = (event) => {
        
    }

    /**
     * @param {PointerEvent} event
     */
    onMouseUp = (event) => {
        
    }

    /**
     * @param {PointerEvent} event
     */
    onMouseLeave = (event) => {
        
    }
}